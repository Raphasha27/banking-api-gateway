server:
  port: 8080
spring:
  application:
    name: banking-api-gateway
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/banking-api-gateway}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
logging:
  level:
    com.banking: INFO
